// Copy to secrets.h (gitignored) and fill in. Never commit secrets.h.
#pragma once

#define WIFI_SSID        "SOULTAKER_B"
#define WIFI_PASS        "change-me"

// EMQX. Account sdigf-edge: publish up/#, status; subscribe down/#; nothing else.
#define MQTT_HOST        "emqx.progrex.tech"
#define MQTT_PORT        1883          // 8883 if MQTT_USE_TLS is 1
#define MQTT_USE_TLS     0             // 1 = WiFiClientSecure (certificate NOT verified — known gap)
#define MQTT_USER        "sdigf-edge"
#define MQTT_PASS        "change-me"
